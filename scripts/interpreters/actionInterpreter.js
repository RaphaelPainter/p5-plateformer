const ActionIDs = {
    MoveToAdjacentScreen: 'MoveToAdjacentScreen',
}

function interpretAction(action, levelContext, param1, param2) {
    console.log(param1, param2)
    if ((action.name = ActionIDs.MoveToAdjacentScreen)) {
    }
}
